/// [FileName] = ../src/filename.ts
/// To Use any algorithem you want

/// Edit that in <package.json> file

/*

  "scripts": {
    "start": "node dist/[fileName].js",
    "dev": "nodemon src/[fileName].ts",
    "build": "tsc -p ."
  },

*/